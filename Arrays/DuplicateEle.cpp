#include<bits/stdc++.h>
using namespace std;

int Key(arr1[],n1)
{
    key=arr1[i]%n1;
}
int main()
{
    int n,key,n1,i;
    cin>>n;
    int arr1[n],arr2[n];
    for( i=0;i<n;i++)
    cin>>arr1[i];

    n1=sizeof(arr1)/sizeof(arr1[0]);

   
       if(arr2[key]!=0)
       {
       arr2[key]=arr1[i];
       }
       else
       Key(arr1[i])


    for(int i=0;i<n;i++)
    cout<<arr2[i]<<" ";


return 0;
}